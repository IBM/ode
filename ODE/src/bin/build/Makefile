PROGRAMS = build${PROG_SUFF}

OFILES   = build${OBJ_SUFF} buildc${OBJ_SUFF} target${OBJ_SUFF}

LIBS     = ${LIB_PREF}${ODELIBNAME}${LIB_SUFF}

IDIR     ?= /bin/
ILIST    = ${PROGRAMS}

.include <${RULES_MK}>
