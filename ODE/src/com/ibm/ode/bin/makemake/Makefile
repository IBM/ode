JAVA_CLASSES = MakeMake${CLASS_SUFF} MakeMakeMenu${CLASS_SUFF} \
               MakeMakeFrame${CLASS_SUFF} MakeMakeOptions${CLASS_SUFF} \
               MakeMakeTextDialog${CLASS_SUFF} MakeMakeFileMaker${CLASS_SUFF} \
               MakeMakeEvent${CLASS_SUFF} MakeMakeVarWindow${CLASS_SUFF} \
               MakeMakeOutputable${CLASS_SUFF} MakeMakeTextArea${CLASS_SUFF} \
               MakeMakePrintWriter${CLASS_SUFF} \
               MakeMakeYesNoDialog${CLASS_SUFF} \
               MakeMakeConfigFile${CLASS_SUFF}

.include <${RULES_MK}>
