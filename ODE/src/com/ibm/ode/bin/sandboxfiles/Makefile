JAVA_CLASSES = SandboxFiles${CLASS_SUFF} \
               SFAboutCmd${CLASS_SUFF} \
               SFAction${CLASS_SUFF} \
               SFDocumentationCmd${CLASS_SUFF} \
               SFEditCmd${CLASS_SUFF} \
               SFExitCmd${CLASS_SUFF} \
               SFFrame${CLASS_SUFF} \
               SFHelpCmd${CLASS_SUFF} \
               SFInfo${CLASS_SUFF} \
               SFMenuItem${CLASS_SUFF} \
               SFRefreshCmd${CLASS_SUFF} \
               SFRunSbls${CLASS_SUFF} \
               SFSblsParser${CLASS_SUFF} \
               SFState${CLASS_SUFF}

.include <${RULES_MK}>
