JAVA_CLASSES = SandboxList${CLASS_SUFF} \
               SLAction${CLASS_SUFF} \
               SLAboutCmd${CLASS_SUFF} \
               SLBuildCmd${CLASS_SUFF} \
               SLCreateCmd${CLASS_SUFF} \
               SLDeleteCmd${CLASS_SUFF} \
               SLDocumentationCmd${CLASS_SUFF} \
               SLExitCmd${CLASS_SUFF} \
               SLFrame${CLASS_SUFF} \
               SLHelpCmd${CLASS_SUFF} \
               SLInfo${CLASS_SUFF} \
               SLListParser${CLASS_SUFF} \
               SLMenuItem${CLASS_SUFF} \
               SLModifyCmd${CLASS_SUFF} \
               SLOpenCmd${CLASS_SUFF} \
               SLRefreshCmd${CLASS_SUFF} \
               SLRetargetCmd${CLASS_SUFF} \
               SLShowFilesCmd${CLASS_SUFF} \
               SLState${CLASS_SUFF} \
               SLWorkonCmd${CLASS_SUFF}

.include <${RULES_MK}>
